#!/bin/bash
#

pushd "$DOTFILES" || exit 1

file="Brewfile"

brew bundle dump --force --file="$file"

content=$(cat "$file")

cat <<EOT > "$file"
# Script generated by $0. DO NOT EDIT.
#
# $(date)
#

EOT

echo "$content" >> "$file"

git diff "$file"

popd || exit 1